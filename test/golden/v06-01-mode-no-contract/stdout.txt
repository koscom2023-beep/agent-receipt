
────────────────────────────────────────────────────────
agent-receipt mode — 지금 이 작업 흐름은?
────────────────────────────────────────────────────────
  계약       : 없음
  git 저장소 : 예
  baseline   : 없음
────────────────────────────────────────────────────────
  ▸ task mode  — 1개 작업 단위
      start → (agent 작업) → verify → check → claims → receipt → reset/commit
      작업마다 receipt 1개. 끝나면 reset 후 다음 작업.
  ▸ daily mode — 하루/브랜치에서 여러 작업 연속
      작업마다 reset → start 로 baseline 을 새로 찍는다(권장).
      누적 추적은 `agent-receipt receipts` 목록으로 (별도 daily 스키마 없음).
────────────────────────────────────────────────────────
  현재       : 아직 모드 이전 — 계약부터.
  다음       : agent-receipt init --preset promptia   (또는 generic)
────────────────────────────────────────────────────────

